<UserControl x:Class="MonthCalendar.DayOfMonth"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MonthCalendar"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Border BorderBrush="Black" BorderThickness="0.5">
        <Grid Background="White">
            <Grid.RowDefinitions>
                <RowDefinition Height="12"></RowDefinition>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="TbDayNumber" Text="1" VerticalAlignment="Top" Width="Auto" FontSize="10" Height="12" />
            <Grid x:Name="GridVacations" Grid.Row="1"></Grid> 
        </Grid>
    </Border>
</UserControl>
